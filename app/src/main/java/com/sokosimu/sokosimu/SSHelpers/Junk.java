package com.sokosimu.sokosimu.SSHelpers;



public class Junk {
}
 /* StringRequest jsArrRequest = new StringRequest
                    (Request.Method.POST, url, new Response.Listener<String>() {

                        @Override
                        public void onResponse(String response) {
                            progressDialog.hide();
                            close();
                        }
                    }, new Response.ErrorListener() {

                        @Override
                        public void onErrorResponse(VolleyError error) {
                            SSToast asdf = new SSToast(""+error.networkResponse.statusCode);
                            progressDialog.hide();
                        }
                    }){
                @Override
                protected Map<String,String> getParams(){
                    Map<String,String> params = new HashMap<String, String>();
                    params.put("name",product_name);
                    params.put("description",product_description);
                    params.put("price",product_price);
                    params.put("category_id",selectedCategoryId);

                   // if(image_one!=null){
                        params.put("image_one",getString(R.string.testString));
                   // }
                    if(image_two!=null){
                        params.put("image_two",image_two);
                    }
                    if(image_three!=null){
                        params.put("image_three",image_three);
                    }

                    return params;
                }
                @Override
                public Map<String, String> getHeaders() throws AuthFailureError {
                    HashMap<String, String> headers = new HashMap<String, String>();
                    headers.put("Content-Type","application/x-www-form-urlencoded");
                    return headers;
                }
            };*/
